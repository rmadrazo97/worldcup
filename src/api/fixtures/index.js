// Tiny safety-net fixtures for offline / pre-config app shell. We export
// empty arrays so the component states naturally fall to their existing
// "Loading…" / "Couldn't load" copy. Real fixtures live in __tests__.

export const FALLBACK_TEAMS = []
export const FALLBACK_GROUPS = []
